import React from 'react'
import Display from './Button/Display'
import RandomNum from './randomNum'

export default function Checker(a, b) {
    if ( a == b) {
        return (
            <div>
                Winner!!!
            </div>
        )
    }
    
}
